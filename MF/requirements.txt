# Required packages for Mutual Fund Recommendation System
# Required packages for Mutual Fund Recommendation System
# Use compatible (non-build-from-source) version constraints to avoid
# issues on newer Python versions (e.g. missing distutils on Python 3.12+).
# Prefer wheels that are commonly available on PyPI for the target runtime.

# Core data and numeric libs
pandas>=2.0.0,<2.2
numpy>=1.25.0,<2.0  # prefer newer numpy wheels that don't require distutils

# Machine learning / statistics
scikit-learn>=1.3.0,<1.4
statsmodels>=0.14.0,<0.15

# Data sources and IO
yfinance>=0.2.28
openpyxl>=3.1.2
python-dateutil>=2.8.2
pytz>=2023.3

# Visualization and UI
matplotlib>=3.7.2
streamlit>=1.25.0
plotly>=5.0.0

# Other
xgboost>=1.7.6
PyPDF2>=3.0.1

# Note: If deployment still fails on Streamlit Cloud with the chosen Python
# runtime, consider pinning a runtime (e.g. Python 3.11) via runtime.txt
# or adding a `pyproject.toml` build-system with setuptools present.